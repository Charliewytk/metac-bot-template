interface:
  display_name: "Metac Bot Template"
  short_description: "Repo-specific patterns and workflows for metac-bot-template"
  default_prompt: "Use the metac-bot-template repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true